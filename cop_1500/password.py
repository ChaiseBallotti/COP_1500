# This will be a password gen
# 1) input the desired length of the password
# 2) the gen will create a password with at least
#       one lower case letter
#       one upper case letter
#       one number
#       one special character

# use a loop with if statements to ensure good input data
def user_input() -> int:
    while True:
        
        length = input("Enter the desired length of you password.")
        #check if input is a string or not
        try:
            if 
        except
    return length

test = user_input()
